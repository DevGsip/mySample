spring:
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27017/template

  datasource:
    name: test
    url: jdbc:mysql://localhost:3306/template?serverTimezone=UTC #url
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.mongo.model
server:
  port: 8062

